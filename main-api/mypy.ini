[mypy]
python_version = 3.14
ignore_missing_imports = True
# SQLAlchemy column attributes look like plain class variables to mypy,
# so assigning to them (e.g. user.email = value) triggers false assignment
# errors. Disabling this error code is the standard workaround.
disable_error_code = assignment